@model QuickSellAdmin.Models.Location


@{
    /**/

    Layout = null;
}



@using (Html.BeginForm("AddOrEdit", "Locations", FormMethod.Post, new { onsubmit = "return SubmitForm(this)" }))
{
    @Html.HiddenFor(model => model.id)
    <div class="form-group">
        @Html.LabelFor(model => model.location1, new { @class = "control-label" })
        @Html.EditorFor(model => model.location1, new { htmlAttributes = new { @class = "form-control" } })
        @Html.ValidationMessageFor(model => model.location1)
    </div>
    
    <div class="form-group">
        <input type="submit" value="Submit" class="btn btn-primary" />
        <input type="reset" value="Reset" class="btn" />
    </div>
}